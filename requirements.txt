# Use CPU wheels for PyTorch
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.37.1

# Core ML stack
scikit-learn==1.5.1
pandas==2.2.2
numpy<2.0
scipy==1.13.1

# Visualization
matplotlib==3.8.4

# Explainability
shap==0.45.1
lime==0.2.0.1

# BERT / Transformers
transformers==4.42.4
tokenizers==0.19.1
huggingface-hub==0.24.6
sentencepiece==0.2.0   # 兼容需要 SentencePiece 的模型；BERT 不一定用到，但装上更稳

# PyTorch (CPU)
torch==2.3.1+cpu
